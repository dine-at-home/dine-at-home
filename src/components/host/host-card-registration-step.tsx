// DISABLED — card-payout (OCT) registration UI. Hosts now provide an IBAN for manual bank
// transfers instead of registering a payout card. The full working component is preserved on
// the `card-payout` git branch (see PAYIN_MANUAL_PAYOUT_PLAN.md).
//
// This stub remains only so the file path keeps existing; nothing imports it in manual mode.

export const CARD_REGISTRATION_RETURN_TO_KEY = 'datthome.cardRegistrationReturnTo'
